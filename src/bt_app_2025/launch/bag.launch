<launch>
    <!-- Declare arguments -->
    <arg name='name_of_bag_file' default='0530-7'/>

    <!-- record bag -->
    <executable cmd="ros2 bag record --output /home/ros/Eurobot-2025-Main/bag/$(var name_of_bag_file) /candidates /scan /goal_pose /local_filter /odom2map /final_pose /lidar_pose /raw_obstacles /raw_obstacles_visualization_pcl /rhino_pose /cmd_vel /mission_status /mission_type /odoo_googoogoo /tf_static /tf" />
</launch>